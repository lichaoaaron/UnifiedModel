kind: entity_set_link
schema:
  url: "mmodel.aliyun.com"
  version: "v0.1.0"
metadata:
  name: "otel.service_depends_on_otel.infra_kafka"
  domain: otel
  display_name:
    en_us: "Service Depends on Kafka"
    zh_cn: "服务依赖 Kafka"
  description:
    en_us: "Services that produce to or consume from Kafka for async event-driven communication."
    zh_cn: "向 Kafka 生产或消费消息以进行异步事件驱动通信的服务。"
spec:
  src:
    domain: otel
    kind: entity_set
    name: otel.service
  dest:
    domain: otel
    kind: entity_set
    name: otel.infra
  entity_link_type: depends_on

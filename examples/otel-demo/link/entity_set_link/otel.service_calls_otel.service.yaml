kind: entity_set_link
schema:
  url: "mmodel.aliyun.com"
  version: "v0.1.0"
metadata:
  name: "otel.service_calls_otel.service"
  domain: otel
  display_name:
    en_us: "Service Calls Service"
    zh_cn: "服务间调用"
  description:
    en_us: "A service calls another service via gRPC or HTTP. Discovered from OpenTelemetry trace spans."
    zh_cn: "一个服务通过 gRPC 或 HTTP 调用另一个服务。从 OpenTelemetry trace span 中发现。"
spec:
  src:
    domain: otel
    kind: entity_set
    name: otel.service
  dest:
    domain: otel
    kind: entity_set
    name: otel.service
  entity_link_type: calls

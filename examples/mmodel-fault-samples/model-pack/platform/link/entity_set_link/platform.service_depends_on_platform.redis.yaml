kind: entity_set_link
schema:
  url: "umodel.aliyun.com"
  version: "v0.1.0"
metadata:
  name: "platform.service_depends_on_platform.redis"
  domain: platform
  display_name:
    en_us: "Service depends on Redis"
    zh_cn: "服务依赖 Redis"
spec:
  src:
    domain: platform
    kind: entity_set
    name: platform.service
  dest:
    domain: platform
    kind: entity_set
    name: platform.redis
  entity_link_type: depends_on
